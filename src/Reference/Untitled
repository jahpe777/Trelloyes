_renderButtons() {
    return STORE.buttons.map(button => {
      return <Button innerText={button.innerText}
                     clicked={this._updateCount.bind(this)}
                     />
    })
  }